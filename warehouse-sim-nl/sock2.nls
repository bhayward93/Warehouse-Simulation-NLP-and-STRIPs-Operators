to connect-clojure
  sock2:connect-local 2222
end

to read
  let text sock2:read
  show (text)
end

to send [message]
    sock2:write (word message)
end

to send-world-state

  send (word "{")
  ask patches[
    send (word "{at '("pxcor " " pycor ")" turtles-here "}")
  ]
  send (word "}")


end
